<?xml version="1.0" encoding="UTF-8"?>
<issues format="6" by="lint 8.7.3" type="baseline" client="gradle" dependencies="true" name="AGP (8.7.3)" variant="all" version="8.7.3">

    <!--
        Sole remaining baselined finding. Lint suggests merging mipmap-anydpi-v26
        into a plain mipmap-anydpi (minSdk 34 > 26), but the launcher resource is an
        <adaptive-icon> with a <monochrome> layer, which AAPT only links under the
        -v26 qualifier. Dropping the qualifier breaks `mipmap/ic_launcher` resource
        linking, so the -v26 folder is intentional and this advisory is a false
        positive we keep baselined rather than regress the build.
    -->
    <issue
        id="ObsoleteSdkInt"
        message="This folder configuration (`v26`) is unnecessary; `minSdkVersion` is 34. Merge all the resources in this folder into `mipmap-anydpi`.">
        <location
            file="src/main/res/mipmap-anydpi-v26"/>
    </issue>

</issues>
