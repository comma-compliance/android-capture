# Robolectric 4.13 bundles Android framework jars through SDK 34; the app targets
# 35 but the unit tests only need a framework org.json + Room sqlite, so run them
# against SDK 34 to avoid the missing-framework-jar SDK picker error.
sdk=34
